---
title: "FigureForAnimalTurnTaking"
author: "Chris Cox"
date: "2025-12-11"
output: pdf_document
editor_options: 
  chunk_output_type: console
---

```{r}
create_modeling_cycle <- function() {
  nodes <- data.frame(
    x = c(0.5, 
          0.5, 
          0.5),
    y = c(0.8,
          0.6,
          0.4),
    label = c(
      "Real World",
      "Mechanistic Theory",
      "Formal Models"
    ),
    label_size = c(10, 10, 10)
  )
  
  # Create plot
  p <- ggplot() +
    # Dashed arrow: Real World <-> Mechanistic Theory
    geom_segment(aes(x = 0.5, y = 0.77, xend = 0.5, yend = 0.65),
                 arrow = arrow(length = unit(0.25, "cm"), type = "closed", ends = "both"),
                 linewidth = 0.8, linetype = "dashed") +
    
    # Theory <-> Models curved arrows
    geom_curve(aes(x = 0.47, y = 0.45, xend = 0.47, yend = 0.55),
               arrow = arrow(length = unit(0.25, "cm"), type = "closed"),
               linewidth = 0.8, curvature = -0.65) +
    geom_curve(aes(x = 0.53, yend = 0.45, xend = 0.53, y = 0.55),
               arrow = arrow(length = unit(0.25, "cm"), type = "closed"),
               linewidth = 0.8, curvature = -0.65) +
    
    # Formal models to empirical studies
    geom_curve(aes(x = 0.4, yend = 0.72, xend = 0.4, y = 0.4),
               arrow = arrow(length = unit(0.25, "cm"), type = "closed"),
               linewidth = 0.8, curvature = -0.65) +
    
    # Scientific Literature + mathematical frameworks
    geom_curve(aes(x = 0.86, yend = 0.33, xend = 0.86, y = 0.67),
               arrow = arrow(length = unit(0.25, "cm"), type = "closed", ends = "both"),
               linewidth = 0.8, curvature = -0.35) +
    
    # Labels
    annotate("text", x = 0.44, y = 0.72, label = "Empirical \n  Studies", size = 4) +
    annotate("text", x = 0.6, y = 0.5, label = "Assumptions", size = 4) +
    annotate("text", x = 0.4, y = 0.5, label = "Quantitative\n Predictions", size = 4) +
    
    annotate("text", x = 0.84, y = 0.3, label = "  Mathematical\n   Frameworks", size = 4) +
    annotate("text", x = 0.84, y = 0.7, label = "Cumulative\n Literature", size = 4) +
    
    # External arrows
    geom_segment(aes(x = 0.63, y = 0.4, xend = 0.8, yend = 0.3), linewidth = 0.8,
                 arrow = arrow(length = unit(0.25, "cm"), type = "closed", ends = "both")) +
    geom_segment(aes(x = 0.63, y = 0.6, xend = 0.8, yend = 0.7), linewidth = 0.8,
                 arrow = arrow(length = unit(0.25, "cm"), type = "closed", ends = "both")) +
    
    # Node labels
    geom_text(data = nodes, aes(x = x, y = y, label = label), size = 8, lineheight = 0.9) +
    
    # Styling
    coord_cartesian(xlim = c(0.3, 0.9), ylim = c(0.3, 0.8)) +
    theme_void() +
    theme(plot.margin = margin(10, 10, 10, 10))
  
  return(p)
}

# Create and save
p <- create_modeling_cycle()
print(p)
ggsave("modeling_cycle.png", p, width = 6, height = 7, dpi = 300, bg = "white")
```


